name: Test and lint the development branch

on:
  push:
    branches:
      - dev
  pull_request:
    branches:
      - dev

jobs:
  build-and-push-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20 # Adjust to match your project's Node.js version
          cache: 'npm'
          cache-dependency-path: ieee-website-app/package-lock.json

      - name: Install dependencies
        run: cd ieee-website-app && npm ci

      - name: Run linter
        run: cd ieee-website-app && npm run lint
